pipeline {
    agent any


    stages {
        stage('build') {
          if ( $GIT_BRANCH=='origin/master' ){
            sh "printenv"
          }
          steps{

            }
          }
    }

}
